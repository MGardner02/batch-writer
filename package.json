{
  "name": "batch-writer",
  "version": "0.0.1",
  "description": "Manages stream flow and maximizes resource utilization for write streams performing operations in batches",
  "license": "MIT",
  "repository": "mgardner02/batch-writer",
  "main": "index.js",
  "keywords": [],
  "scripts": {
    "test": "nyc mocha",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "codecov": "^3.2.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1"
  }
}
